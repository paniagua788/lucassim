---
// src/components/portfolio/EducationCerts.astro
// Educación y certificaciones en dos columnas. Bilingüe.
---

<!-- ESPAÑOL -->
<section data-lang="es" class="py-12 px-6 max-w-4xl mx-auto">
  <div class="grid sm:grid-cols-2 gap-8">

    <!-- Educación -->
    <div>
      <h2 class="text-xs text-muted uppercase tracking-widest mb-6">Educación</h2>
      <div class="flex flex-col gap-4">
        <div>
          <p class="font-semibold text-white text-sm">Ingeniería de Sistemas con énfasis en TICs</p>
          <p class="text-xs text-muted">Facultad Politécnica — UNE</p>
        </div>
        <div>
          <p class="font-semibold text-white text-sm">Postgrado en Didáctica Universitaria</p>
          <p class="text-xs text-muted">Escuela de Posgrado — UNE</p>
        </div>
        <div>
          <p class="font-semibold text-white text-sm">Diplomado en Administración de Sistemas Linux</p>
          <p class="text-xs text-muted">UAA — Universidad Autónoma de Asunción</p>
        </div>
        <div>
          <p class="font-semibold text-white text-sm">Diplomado en Herramientas Forenses</p>
          <p class="text-xs text-muted">Corte Suprema de Justicia</p>
        </div>
      </div>
    </div>

    <!-- Certificaciones -->
    <div>
      <h2 class="text-xs text-muted uppercase tracking-widest mb-6">Certificaciones</h2>
      <div class="flex flex-col gap-5">

        <div>
          <p class="text-xs font-semibold text-blue-400 mb-2">Cloud & Infraestructura</p>
          <ul class="flex flex-col gap-1.5">
            {[
              ['AWS Certified Cloud Practitioner', 'Amazon Web Services'],
              ['Microsoft Azure Fundamentals (AZ-900)', 'Microsoft'],
              ['Microsoft 365 Fundamentals (MS-900)', 'Microsoft'],
              ['Linux Essentials', 'Linux Professional Institute (LPI)'],
              ['RHCSA Rapid Track (RH199)', 'Red Hat'],
              ['CCNA: Introduction to Networks', 'Cisco'],
              ['Tecnólogo de Redes', 'SNPP'],
              ['Cloud Computing', 'SNPP'],
            ].map(([name, issuer]) => (
              <li class="text-xs text-muted">
                <span class="font-medium text-white">{name}</span>
                <span class="text-subtle"> — {issuer}</span>
              </li>
            ))}
          </ul>
        </div>

        <div>
          <p class="text-xs font-semibold text-blue-400 mb-2">Gestión de Servicios TI</p>
          <ul class="flex flex-col gap-1.5">
            {[
              ['ITIL 4 Foundation Certificate in ITSM', 'AXELOS'],
              ['Confluence Fundamentals', 'Atlassian'],
              ['Jira Fundamentals', 'Atlassian'],
            ].map(([name, issuer]) => (
              <li class="text-xs text-muted">
                <span class="font-medium text-white">{name}</span>
                <span class="text-subtle"> — {issuer}</span>
              </li>
            ))}
          </ul>
        </div>

        <div>
          <p class="text-xs font-semibold text-blue-400 mb-2">Desarrollo & Metodologías</p>
          <ul class="flex flex-col gap-1.5">
            {[
              ['Python Coding Bootcamp', 'Penguin Academy'],
              ['Scrum Foundation Professional', 'CertiProf'],
            ].map(([name, issuer]) => (
              <li class="text-xs text-muted">
                <span class="font-medium text-white">{name}</span>
                <span class="text-subtle"> — {issuer}</span>
              </li>
            ))}
          </ul>
        </div>

        <div>
          <p class="text-xs font-semibold text-blue-400 mb-2">Seguridad</p>
          <ul class="flex flex-col gap-1.5">
            {[
              ['ISO 27001 Internal Auditor Bootcamp', 'Hacker Mentor'],
              ['Cybersecurity Essentials', 'Cisco Networking Academy'],
              ['Introduction to Cybersecurity', 'Cisco Networking Academy'],
            ].map(([name, issuer]) => (
              <li class="text-xs text-muted">
                <span class="font-medium text-white">{name}</span>
                <span class="text-subtle"> — {issuer}</span>
              </li>
            ))}
          </ul>
        </div>

        <div>
          <p class="text-xs font-semibold text-blue-400 mb-2">Redes e IoT</p>
          <ul class="flex flex-col gap-1.5">
            {[
              ['Introduction to IoT', 'Cisco Networking Academy'],
            ].map(([name, issuer]) => (
              <li class="text-xs text-muted">
                <span class="font-medium text-white">{name}</span>
                <span class="text-subtle"> — {issuer}</span>
              </li>
            ))}
          </ul>
        </div>

        <div>
          <p class="text-xs font-semibold text-blue-400 mb-2">Idiomas</p>
          <ul class="flex flex-col gap-1.5">
            {[
              ['EF SET English Certificate C1 Advanced (64/100)', 'EF SET'],
            ].map(([name, issuer]) => (
              <li class="text-xs text-muted">
                <span class="font-medium text-white">{name}</span>
                <span class="text-subtle"> — {issuer}</span>
              </li>
            ))}
          </ul>
        </div>

      </div>
    </div>

  </div>
</section>

<!-- ENGLISH -->
<section data-lang="en" class="hidden py-12 px-6 max-w-4xl mx-auto">
  <div class="grid sm:grid-cols-2 gap-8">

    <!-- Education -->
    <div>
      <h2 class="text-xs text-muted uppercase tracking-widest mb-6">Education</h2>
      <div class="flex flex-col gap-4">
        <div>
          <p class="font-semibold text-white text-sm">Systems Engineering with emphasis in ICTs</p>
          <p class="text-xs text-muted">Facultad Politécnica — UNE</p>
        </div>
        <div>
          <p class="font-semibold text-white text-sm">Postgraduate in University Didactics</p>
          <p class="text-xs text-muted">Escuela de Posgrado — UNE</p>
        </div>
        <div>
          <p class="font-semibold text-white text-sm">Diploma in Linux Systems Administration</p>
          <p class="text-xs text-muted">UAA — Universidad Autónoma de Asunción</p>
        </div>
        <div>
          <p class="font-semibold text-white text-sm">Diploma in Forensic Tools</p>
          <p class="text-xs text-muted">Corte Suprema de Justicia</p>
        </div>
      </div>
    </div>

    <!-- Certifications -->
    <div>
      <h2 class="text-xs text-muted uppercase tracking-widest mb-6">Certifications</h2>
      <div class="flex flex-col gap-5">

        <div>
          <p class="text-xs font-semibold text-blue-400 mb-2">Cloud & Infrastructure</p>
          <ul class="flex flex-col gap-1.5">
            {[
              ['AWS Certified Cloud Practitioner', 'Amazon Web Services'],
              ['Microsoft Azure Fundamentals (AZ-900)', 'Microsoft'],
              ['Microsoft 365 Fundamentals (MS-900)', 'Microsoft'],
              ['Linux Essentials', 'Linux Professional Institute (LPI)'],
              ['RHCSA Rapid Track (RH199)', 'Red Hat'],
              ['CCNA: Introduction to Networks', 'Cisco'],
              ['Tecnólogo de Redes', 'SNPP'],
              ['Cloud Computing', 'SNPP'],
            ].map(([name, issuer]) => (
              <li class="text-xs text-muted">
                <span class="font-medium text-white">{name}</span>
                <span class="text-subtle"> — {issuer}</span>
              </li>
            ))}
          </ul>
        </div>

        <div>
          <p class="text-xs font-semibold text-blue-400 mb-2">IT Service Management</p>
          <ul class="flex flex-col gap-1.5">
            {[
              ['ITIL 4 Foundation Certificate in ITSM', 'AXELOS'],
              ['Confluence Fundamentals', 'Atlassian'],
              ['Jira Fundamentals', 'Atlassian'],
            ].map(([name, issuer]) => (
              <li class="text-xs text-muted">
                <span class="font-medium text-white">{name}</span>
                <span class="text-subtle"> — {issuer}</span>
              </li>
            ))}
          </ul>
        </div>

        <div>
          <p class="text-xs font-semibold text-blue-400 mb-2">Development & Methodologies</p>
          <ul class="flex flex-col gap-1.5">
            {[
              ['Python Coding Bootcamp', 'Penguin Academy'],
              ['Scrum Foundation Professional', 'CertiProf'],
            ].map(([name, issuer]) => (
              <li class="text-xs text-muted">
                <span class="font-medium text-white">{name}</span>
                <span class="text-subtle"> — {issuer}</span>
              </li>
            ))}
          </ul>
        </div>

        <div>
          <p class="text-xs font-semibold text-blue-400 mb-2">Security</p>
          <ul class="flex flex-col gap-1.5">
            {[
              ['ISO 27001 Internal Auditor Bootcamp', 'Hacker Mentor'],
              ['Cybersecurity Essentials', 'Cisco Networking Academy'],
              ['Introduction to Cybersecurity', 'Cisco Networking Academy'],
            ].map(([name, issuer]) => (
              <li class="text-xs text-muted">
                <span class="font-medium text-white">{name}</span>
                <span class="text-subtle"> — {issuer}</span>
              </li>
            ))}
          </ul>
        </div>

        <div>
          <p class="text-xs font-semibold text-blue-400 mb-2">Networking & IoT</p>
          <ul class="flex flex-col gap-1.5">
            {[
              ['Introduction to IoT', 'Cisco Networking Academy'],
            ].map(([name, issuer]) => (
              <li class="text-xs text-muted">
                <span class="font-medium text-white">{name}</span>
                <span class="text-subtle"> — {issuer}</span>
              </li>
            ))}
          </ul>
        </div>

        <div>
          <p class="text-xs font-semibold text-blue-400 mb-2">Languages</p>
          <ul class="flex flex-col gap-1.5">
            {[
              ['EF SET English Certificate C1 Advanced (64/100)', 'EF SET'],
            ].map(([name, issuer]) => (
              <li class="text-xs text-muted">
                <span class="font-medium text-white">{name}</span>
                <span class="text-subtle"> — {issuer}</span>
              </li>
            ))}
          </ul>
        </div>

      </div>
    </div>

  </div>
</section>
