---
// src/components/portfolio/ProjectsList.astro
// Proyectos propios con stack y descripción. Bilingüe.
---

<!-- ESPAÑOL -->
<section data-lang="es" class="py-12 px-6 max-w-4xl mx-auto">
  <h2 class="text-xs text-muted uppercase tracking-widest mb-6">Proyectos Propios</h2>
  <p class="text-xs text-subtle mb-6">lucassim.com.py</p>

  <div class="flex flex-col gap-5">

    <!-- VareApp -->
    <div class="bg-surface border border-border rounded-xl p-6">
      <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-2 mb-3">
        <div>
          <p class="font-bold text-white text-lg">VareApp</p>
          <p class="text-sm text-muted">SaaS de pedidos gastronómicos para restaurantes y food trucks</p>
        </div>
      </div>
      <div class="flex flex-wrap gap-1.5 mb-4">
        {['Python', 'Flask', 'MongoDB', 'TailwindCSS', 'Cloudinary', 'Docker', 'WhatsApp API'].map(t => (
          <span class="px-2 py-0.5 bg-blue-900/30 text-blue-400 rounded text-xs font-medium">{t}</span>
        ))}
      </div>
      <ul class="text-sm text-muted flex flex-col gap-1.5 list-disc list-inside">
        <li>Sistema web multi-tenant con catálogo público, checkout e integración WhatsApp. Arquitectura de servicios desacoplada con Marshmallow para validación.</li>
        <li>Módulos: caja con IVA y factura electrónica (SIFEN Paraguay), inventario con alertas de stock, dashboard de métricas (plan Pro), roles admin/staff y paleta de colores personalizable por tenant.</li>
        <li>Gestión de categorías con drag &amp; drop, variantes de producto, límites de stock diario, horarios semanales y cierres especiales.</li>
      </ul>
    </div>

    <!-- Slotr -->
    <div class="bg-surface border border-border rounded-xl p-6">
      <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-2 mb-3">
        <div>
          <p class="font-bold text-white text-lg">Slotr</p>
          <p class="text-sm text-muted">SaaS de agendamiento online multi-tenant</p>
        </div>
      </div>
      <div class="flex flex-wrap gap-1.5 mb-4">
        {['Python', 'Flask', 'PostgreSQL', 'Telegram Bot API', 'Docker', 'Render', 'CI/CD'].map(t => (
          <span class="px-2 py-0.5 bg-blue-900/30 text-blue-400 rounded text-xs font-medium">{t}</span>
        ))}
      </div>
      <ul class="text-sm text-muted flex flex-col gap-1.5 list-disc list-inside">
        <li>Plataforma de turnos online para clínicas, peluquerías y canchas deportivas con múltiples negocios activos en producción.</li>
        <li>Reducción del 60% en tiempos de espera para los clientes finales.</li>
        <li>Calendarios por recurso/profesional, notificaciones Telegram, analytics dashboard y CI/CD automatizado en Render.</li>
      </ul>
    </div>

    <!-- MedicFile -->
    <div class="bg-surface border border-border rounded-xl p-6">
      <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-2 mb-3">
        <div>
          <p class="font-bold text-white text-lg">MedicFile</p>
          <p class="text-sm text-muted">Ganador Hackathon Penguin Academy 2023</p>
        </div>
        <span class="px-2 py-0.5 bg-yellow-900/30 text-yellow-400 rounded text-xs font-medium whitespace-nowrap">🏆 Hackathon</span>
      </div>
      <div class="flex flex-wrap gap-1.5 mb-4">
        {['Flask', 'SQLAlchemy', 'TailwindCSS', 'QR Code', 'Pillow'].map(t => (
          <span class="px-2 py-0.5 bg-blue-900/30 text-blue-400 rounded text-xs font-medium">{t}</span>
        ))}
      </div>
      <ul class="text-sm text-muted flex flex-col gap-1.5 list-disc list-inside">
        <li>App web de registros médicos de emergencia con generación dinámica de código QR para acceso rápido a información crítica.</li>
        <li>Diseño mobile-first, APIs REST con validación completa.</li>
      </ul>
    </div>

    <!-- Herramientas -->
    <div class="bg-surface border border-border rounded-xl p-6">
      <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-2 mb-3">
        <div>
          <p class="font-bold text-white text-lg">Herramientas de Infraestructura & APIs</p>
          <p class="text-sm text-muted">Utilidades internas y scripts de automatización</p>
        </div>
      </div>
      <div class="flex flex-wrap gap-1.5 mb-4">
        {['PowerShell', 'Python', 'Flask', 'BeautifulSoup', 'SMTP', 'Active Directory'].map(t => (
          <span class="px-2 py-0.5 bg-blue-900/30 text-blue-400 rounded text-xs font-medium">{t}</span>
        ))}
      </div>
      <ul class="text-sm text-muted flex flex-col gap-1.5 list-disc list-inside">
        <li>Web Scraping API: API Flask para consultas de datos financieros con BeautifulSoup y cobertura de tests con Pytest.</li>
        <li>Mailer API: servicio de envío de emails con Flask y SMTP, validación de entradas y manejo de errores.</li>
        <li>AD Query Tool: herramienta CLI en PowerShell para consultas automatizadas en Active Directory.</li>
        <li><strong class="text-white">Automation Scripts:</strong> colección de scripts Python y PowerShell para gestión de infraestructura y procesos de soporte, reduciendo tareas manuales en 10+ horas/semana.</li>
      </ul>
    </div>

  </div>
</section>

<!-- ENGLISH -->
<section data-lang="en" class="hidden py-12 px-6 max-w-4xl mx-auto">
  <h2 class="text-xs text-muted uppercase tracking-widest mb-6">Own Projects</h2>
  <p class="text-xs text-subtle mb-6">lucassim.com.py</p>

  <div class="flex flex-col gap-5">

    <!-- VareApp -->
    <div class="bg-surface border border-border rounded-xl p-6">
      <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-2 mb-3">
        <div>
          <p class="font-bold text-white text-lg">VareApp</p>
          <p class="text-sm text-muted">Gastronomic ordering SaaS for restaurants and food trucks</p>
        </div>
      </div>
      <div class="flex flex-wrap gap-1.5 mb-4">
        {['Python', 'Flask', 'MongoDB', 'TailwindCSS', 'Cloudinary', 'Docker', 'WhatsApp API'].map(t => (
          <span class="px-2 py-0.5 bg-blue-900/30 text-blue-400 rounded text-xs font-medium">{t}</span>
        ))}
      </div>
      <ul class="text-sm text-muted flex flex-col gap-1.5 list-disc list-inside">
        <li>Multi-tenant web system with public catalog, checkout, and WhatsApp integration. Decoupled service architecture with Marshmallow for validation.</li>
        <li>Modules: POS with VAT and electronic invoicing (SIFEN Paraguay), inventory with stock alerts, metrics dashboard (Pro plan), admin/staff roles, and customizable color palette per tenant.</li>
        <li>Drag &amp; drop category management, product variants, daily stock limits, weekly schedules, and special closures.</li>
      </ul>
    </div>

    <!-- Slotr -->
    <div class="bg-surface border border-border rounded-xl p-6">
      <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-2 mb-3">
        <div>
          <p class="font-bold text-white text-lg">Slotr</p>
          <p class="text-sm text-muted">Multi-tenant online scheduling SaaS</p>
        </div>
      </div>
      <div class="flex flex-wrap gap-1.5 mb-4">
        {['Python', 'Flask', 'PostgreSQL', 'Telegram Bot API', 'Docker', 'Render', 'CI/CD'].map(t => (
          <span class="px-2 py-0.5 bg-blue-900/30 text-blue-400 rounded text-xs font-medium">{t}</span>
        ))}
      </div>
      <ul class="text-sm text-muted flex flex-col gap-1.5 list-disc list-inside">
        <li>Online appointment platform for clinics, barber shops, and sports courts with multiple active businesses in production.</li>
        <li>60% reduction in customer wait times.</li>
        <li>Per-resource/professional calendars, Telegram notifications, analytics dashboard, and automated CI/CD on Render.</li>
      </ul>
    </div>

    <!-- MedicFile -->
    <div class="bg-surface border border-border rounded-xl p-6">
      <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-2 mb-3">
        <div>
          <p class="font-bold text-white text-lg">MedicFile</p>
          <p class="text-sm text-muted">Winner — Penguin Academy Hackathon 2023</p>
        </div>
        <span class="px-2 py-0.5 bg-yellow-900/30 text-yellow-400 rounded text-xs font-medium whitespace-nowrap">🏆 Hackathon</span>
      </div>
      <div class="flex flex-wrap gap-1.5 mb-4">
        {['Flask', 'SQLAlchemy', 'TailwindCSS', 'QR Code', 'Pillow'].map(t => (
          <span class="px-2 py-0.5 bg-blue-900/30 text-blue-400 rounded text-xs font-medium">{t}</span>
        ))}
      </div>
      <ul class="text-sm text-muted flex flex-col gap-1.5 list-disc list-inside">
        <li>Emergency medical record web app with dynamic QR code generation for quick access to critical information.</li>
        <li>Mobile-first design, REST APIs with full validation.</li>
      </ul>
    </div>

    <!-- Herramientas -->
    <div class="bg-surface border border-border rounded-xl p-6">
      <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-2 mb-3">
        <div>
          <p class="font-bold text-white text-lg">Infrastructure Tools & APIs</p>
          <p class="text-sm text-muted">Internal utilities and automation scripts</p>
        </div>
      </div>
      <div class="flex flex-wrap gap-1.5 mb-4">
        {['PowerShell', 'Python', 'Flask', 'BeautifulSoup', 'SMTP', 'Active Directory'].map(t => (
          <span class="px-2 py-0.5 bg-blue-900/30 text-blue-400 rounded text-xs font-medium">{t}</span>
        ))}
      </div>
      <ul class="text-sm text-muted flex flex-col gap-1.5 list-disc list-inside">
        <li>Web Scraping API: Flask API for financial data queries using BeautifulSoup with Pytest test coverage.</li>
        <li>Mailer API: email sending service with Flask and SMTP, input validation, and error handling.</li>
        <li>AD Query Tool: PowerShell CLI for automated Active Directory queries.</li>
        <li><strong class="text-white">Automation Scripts:</strong> collection of Python and PowerShell scripts for infrastructure management and support processes, reducing manual tasks by 10+ hours/week.</li>
      </ul>
    </div>

  </div>
</section>
